<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Page</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="bg-gray-800 min-h-screen flex items-center p-8">
    <div>

        <div class="p-4">
            <a href="/logout" class="text-white text-center px-2 py-1 rounded-md hover:underline bg-red-500">Logout</a>
            <h2 class="text-xl font-bold mb-4 ">Welcome!<span class="text-blue-500"> @<%= user.username %></span></h2>
            <p class="mb-2">Age: <%= user.age %>
            </p>
            <p>Create Your Post</p>
        </div>
        <div class="p-4 border-t border-gray-300 flex items-center space-x-2">
            <form action="/post" method="POST" class="flex gap-4 flex-col">
                <textarea name="content" id="" placeholder="What's in your mind"
                    class="border-1 rounded-md p-2 inline-block outline-none w-[300px]" required="true"></textarea>
                <input type=submit value="Create a Post"
                    class="text-white text-center px-2 py-1 rounded-md hover:underline bg-blue-500">
            </form>



        </div>
        <div class="posts">
            <h1>Your Posts</h1>
            <div class="postconatiner">
                <% user.posts.reverse().forEach((post)=>{ %>

                    <div class="bg-gray-700 border-2 border-black w-[300px] p-4 m-4 rounded-md">
                        <h3>@<%= user.username %></h3>
                        <p><%= post.content %></p>
                        <a href="" class=" mr-2 text-blue-500 hover:underline">Like</a>
                        <a href="" class="text-gray-500 hover:underline">Edit</a>
                    </div>
                   <%
                  })   %>

            </div>
        </div>
    </div>






</body>

</html>